"🔨 build angular apps":
  image: node:24.11.0@sha256:e5bbac0e9b8a6e3b96a86a82bbbcf4c533a879694fd613ed616bae5116f6f243
  stage: 🔨 build

  cache:
    key:
      files:
        - package-lock.json
        - package.json
    paths:
      - node_modules/
    policy: pull-push

  before_script:
    - |
      set -x
      if [ ! -d "./node_modules" ]; then
        echo >&2 'node_modules cache not available. run ci.'
        npm ci --prefer-offline --quiet
      fi

  script:
    - |
      npm run lint

      echo >&2 "🔨 Building budget-dataetat-dev"
      npm run build:budget-dev

      echo >&2 "🔨 Building budget-dataetat"
      npm run build:budget

      echo >&2 "🔨 Building france-relance"
      npm run build:france-relance

      echo >&2 "🔨 Building data-qpv"
      npm run build:data-qpv

      set +x

  artifacts:
    expire_in: 30 mins
    paths:
      - dist/

  rules:
    - if: '$DO_BUILD == "✅"'
    - if: '$DO_BUILD == "⚙️"'
      when: manual
    - when: never