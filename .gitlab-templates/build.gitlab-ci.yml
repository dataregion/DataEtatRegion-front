"🔨 build angular apps":
  image: node:24.16.0@sha256:8530f76a96d88820d288761f022e318970dda93d01536919fbc16076b7983e63
  stage: 🔨 build

  cache:
    key:
      files:
        - package-lock.json
        - package.json
    paths:
      - node_modules/
    policy: pull-push

  before_script:
    - |
      set -x
      if [ ! -d "./node_modules" ]; then
        echo >&2 'node_modules cache not available. run ci.'
        npm ci --prefer-offline --quiet
      fi

  script:
    - |
      npm run lint

      echo >&2 "🔨 Building budget-dataetat-dev"
      npm run build:budget-dev

      echo >&2 "🔨 Building budget-dataetat"
      npm run build:budget

      echo >&2 "🔨 Building france-relance"
      npm run build:france-relance

      echo >&2 "🔨 Building data-qpv"
      npm run build:data-qpv

      set +x

  artifacts:
    expire_in: 30 mins
    paths:
      - dist/

  rules:
    - if: '$DO_BUILD == "✅"'
    - if: '$DO_BUILD == "⚙️"'
      when: manual
    - when: never